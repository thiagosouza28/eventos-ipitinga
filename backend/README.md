# Backend

API independente em Express, Prisma e MySQL.

```bash
npm install
npm run prisma:migrate
npm run dev
```

A API local fica em `http://localhost:3001/api` e a verificação de saúde em `/api/health`.
Use `.env.example` como referência e nunca envie o arquivo `.env` ao repositório.
