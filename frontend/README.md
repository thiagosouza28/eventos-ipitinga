# Frontend

Aplicação Next.js independente. Ela não possui acesso ao banco e consome exclusivamente a API configurada em `NEXT_PUBLIC_API_URL`.

```bash
npm install
npm run dev
```

O frontend local fica em `http://localhost:3000`. Consulte `.env.example` para configurar a API.
